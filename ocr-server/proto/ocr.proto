syntax = "proto3";

package ocr;

service OCREngine {
    rpc ExtractText(ExtractRequest) returns (ExtractResponse);
}

message ExtractRequest {
    string id = 1;
    string file_path = 2;
    string file_type = 3;
}

message ExtractResponse {
    string text = 1;
}
